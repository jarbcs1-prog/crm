{
  "name": "crm-demo",
  "version": "0.1.2",
  "private": true,
  "description": "Standalone SQLite demo of the CRM — no auth, no Postgres, no Bun required. Seeded test contact profiles included for SIP testing.",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node server.mjs",
    "reseed": "node server.mjs --reseed"
  },
  "license": "AGPL-3.0-only"
}
