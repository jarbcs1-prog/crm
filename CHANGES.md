# Changes from the Original CRM Repository

This document records every modification made to produce the No-Docker version. The original repository is assumed to be the version at `F:\crm` (the source monorepo with Docker support). All changes are dated **2026-08-24** unless otherwise noted.
---

## 1. Removed Docker Dependency

### What changed

**Removed files:**

- `docker-compose.yml` — the Docker Compose definition that started a PostgreSQL 17 container. Its content was:

  ```yaml
  services:
    postgres:
      image: postgres:17-alpine
      container_name: crm-postgres
      restart: unless-stopped
      environment:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: postgres
        POSTGRES_DB: crm
      ports:
        - "5432:5432"
      volumes:
        - crm-postgres:/var/lib/postgresql/data
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U postgres -d crm"]
        interval: 3s
        timeout: 5s
        retries: 20
    volumes:
      crm-postgres:
  ```

- From `eve-0.31.2/` — the local eve distribution used as a reference. This directory is ~80 MB and it contained Docker-related files (`Dockerfile`, `.dockerignore`, `docker-*.ts`, etc.) that are not part of the CRM itself.
- `_exclude_dirs.txt` — temporary file used during the copy process (not part of the deliverable).

### Why

The original project defaulted to Docker for database setup because it is the zero-configuration path. However, the project never required Docker — `DATABASE_URL` accepts any Postgres connection string and the migration/seed toolchain works with any reachable database. This copy removes the Docker assumption so the CRM can run against an existing local or hosted Postgres with no container runtime.

### Effect on user workflow

Instead of `docker compose up -d` to get a database, the user must:

1. Have a Postgres database already running, **or** start one using platform-native tooling.
2. Set `DATABASE_URL` in `.env` to point at that database.
3. Ensure the target database exists before running `bun run db:deploy`.

The rest of the workflow (`bun install`, `bun run db:deploy`, `bun run db:seed`, `bun run dev`) is unchanged.
---

## 2. Removed Sensitive Credentials

### What changed

**Removed files:**

- `Eve_CRM_client_secret_649581149481-o5d31il0v8rt1ud985es3ovv9re0coi7.apps.googleusercontent.com.json` — a Google OAuth client secret file. This file contains a confidential client secret and must never be committed to a public repository.

### Why

The file is a literal secret, not an example. Its presence in the source tree is a security risk. The OAuth client ID and secret are meant to be configured by the operator in `.env` via `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`. The JSON file was never referenced by the application code — it appears to be a local credential artifact.

### Effect on user workflow

The user must obtain their own Google OAuth credentials through the Google Cloud Console and set `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` in `.env`. The full procedure is documented in [INSTALL.md](./INSTALL.md) and the original README's "Getting the Google OAuth client" section.
---

## 3. Removed Diagnostic Artifacts

### What changed

**Removed files:**

- `error_output.txt` — a build error log from an earlier session (56,822 bytes). Diagnostic only.
- `session_cookie.md` — a short diagnostic note (111 bytes). Not part of the deliverable.

### Why

These files were produced during debugging and are not part of the project. They contain no useful documentation and should not be distributed.
---

## 4. Removed Build Artifacts and Cache Directories

### What changed

**Excluded directories (present in source, not copied):**

- `node_modules/` — installed dependencies. Will be recreated by `bun install` on the target machine.
- `.turbo/` — Turborepo build cache.
- `.eve/` — eve build output (agent build artifacts).
- `.output/` — agent output directory.
- `reports/` — reports directory (contents unknown; excluded to keep the copy clean).
- `.agents/` — agent-specific tooling directory at the repo root.

### Why

These are generated artifacts, caches or machine-specific directories. They are large, platform-dependent and should be regenerated by the build/install process on the target machine. Including them would bloat the repository and could introduce platform-specific state.

### Effect on user workflow

The user must run `bun install` after cloning to recreate `node_modules/`. The first `bun run dev` or `bun run build` will regenerate `.eve/` and `.output/` as needed.
---

## 5. Updated README.md

### What changed

**Modified file:** `README.md`

**Summary of changes:**

| Section | Original text | New text |
|---------|---------------|----------|
| Quick start prerequisites | "You need [Bun](https://bun.com) and Docker." | "You need [Bun](https://bun.com) and a running Postgres database." |
| Quick start commands | Included `docker compose up -d` | Removed the Docker command; added a note that no Docker is required |
| Database setup note | "`DATABASE_URL` already matches the `docker compose` Postgres, so leave it alone unless you brought your own." | "`DATABASE_URL` must point at your Postgres. The shape is: `postgresql://postgres:PASSWORD@HOST:PORT/crm?schema=public`" |
| Sandbox row in stack table | "Docker or microsandbox locally" | "microsandbox locally" |
| "No Docker required" callout | Not present | Added a highlighted note after the Quick Start commands: "This version uses whatever Postgres you have — a local install, one you already host or a managed database. The original repo included a docker-compose.yml for convenience; this copy removes it and works with any Postgres you point DATABASE_URL at." |

The new README also includes a cross-reference to [INSTALL.md](./INSTALL.md) for the full cross-platform setup.

### Why

The original README assumed Docker as the database provider. Since this version removes Docker, the README must tell the user how to connect to their own Postgres instead.
---

## 6. Added New Documentation Files

### INSTALL.md (new file)

A step-by-step installation guide covering:

- **All three target operating systems:** Windows, Linux (Ubuntu/Debian, Fedora/RHEL, Arch), macOS
- **Two Postgres paths per OS:** local install and existing/remote database
- **Platform-native tooling** for each OS (no Docker commands)
- **Complete Postgres setup** including installation commands, service management, user/database creation and authentication configuration
- **Postgres verification** commands for each platform
- **OAuth setup** (Google Cloud Console, redirect URIs, API enablement)
- **Environment configuration** with the full variable table and the exact `DATABASE_URL` shape
- **Post-install verification** — how to confirm the database is reachable
- **Troubleshooting** section per OS
- **Credential handling note** — all passwords and secrets are to be set as environment variables, never hardcoded

### README.md (rewritten)

See Section 5 above for details.
---

## 7. .gitignore Considerations

### What changed

The original `.gitignore` at `F:\crm\.gitignore` is copied as-is to the new location. It already excludes:

- `node_modules`
- `.env` and `.env.*` (except `.env.example`)
- `.turbo`
- `.next/`, `out/`, `build`, `dist`
- `.vercel`
- `*.pem`
- `.eve`, `.output`
- `.DS_Store`

No changes were made to `.gitignore` because the existing rules already cover the artifacts that should not be committed.

### Additional recommendation for the uploader

Before pushing to GitHub, verify that the following are **not** present in the cloned copy:

- Any `.env` file with real credentials (only `.env.example` should be committed)
- Any `node_modules/` directory (should be regenerated by `bun install`)
- Any `.eve/` or `.output/` directories (build artifacts)
- Any `*.pem` files (private keys)
- Any Google OAuth client secret JSON files
---

## 8. Files That Remain Unchanged

The following categories of files are copied verbatim from the original with no modifications:

- All application source code (`apps/`, `packages/`)
- All Prisma schema, migrations and seed files (`packages/db/prisma/`)
- All documentation in `docs/` (agent.md, api.md, crm-plan.md, design.md, environment.md, etc.)
- All configuration files (`turbo.json`, `biome.jsonc`, `skills-lock.json`, `package.json`, etc.)
- All test files
- `LICENSE`, `SECURITY.md`, `CONTRIBUTING.md`, `AGENTS.md`
- All image assets in `docs/images/`
- All shadcn/ui components and Tailwind configuration

The application behavior is identical to the original. Only the database provisioning path and the documentation have changed.
---

## 9. PostgreSQL Authentication Note

### Background

The original `docker-compose.yml` used `POSTGRES_PASSWORD: postgres` and created a database named `crm` owned by the `postgres` user. The `.env.example` file references `DATABASE_URL= "postgresql://postgres:***@localhost:5432/crm?schema=public"`.

### What the operator must do

When using a local Postgres install (rather than the Docker container), the operator must ensure:

1. **The `postgres` role has a known password.** If the local install uses a different password, set it via `ALTER USER postgres PASSWORD 'newpassword';` or use the existing password in `DATABASE_URL`.

2. **The `crm` database exists.** Create it with `CREATE DATABASE crm;` if it does not exist.

3. **`DATABASE_URL` in `.env` reflects the actual credentials.** The connection string must match the local Postgres configuration.

These steps are documented in [INSTALL.md](./INSTALL.md) for each supported OS.

### Security note

The password `postgres` is a default and should be changed on any real deployment. The `.env.example` file uses `***` as a placeholder for the password, signaling that it must be set by the operator.
The INSTALL.md reinforces this: credentials belong in environment variables, never in documentation or code.
---

## 10. Summary of File Changes

| File | Action | Reason |
|------|--------|--------|
| `docker-compose.yml` | **Removed** | Docker not required; Postgres comes from the user's own install |
| `eve-0.31.2/` (directory) | **Removed** | Local reference distribution, not part of CRM; ~80 MB of irrelevant files |
| `error_output.txt` | **Removed** | Diagnostic artifact, not part of deliverable |
| `session_cookie.md` | **Removed** | Diagnostic artifact, not part of deliverable |
| `Eve_CRM_client_secret_...json` | **Removed** | Google OAuth client secret — must never be committed |
| `node_modules/` | **Excluded** | Generated by `bun install`; platform-specific |
| `.turbo/` | **Excluded** | Build cache; regenerated by build |
| `.eve/` | **Excluded** | Build artifacts; regenerated by build |
| `.output/` | **Excluded** | Build artifacts; regenerated by build |
| `reports/` | **Excluded** | Unknown contents; excluded for cleanliness |
| `.agents/` | **Excluded** | Agent tooling directory; not part of CRM source |
| `README.md` | **Rewritten** | Updated for no-Docker workflow; added cross-reference to INSTALL.md |
| `INSTALL.md` | **Created** | New file — comprehensive cross-platform installation guide |
| All other files | **Copied unchanged** | Source code, config, docs, tests, assets — no modifications |
---

## 11. Verifying the Copy

To verify that the No-Docker copy is functional:

```sh
# From F:\crm_nodocker (or the cloned copy on the target machine)

# 1. Install dependencies
bun install

# 2. Configure .env (copy from .env.example and fill in real values)
cp .env.example .env
# Edit .env: set DATABASE_URL, BETTER_AUTH_SECRET, ALLOWED_SIGN_IN,
# and optionally GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET

# 3. Apply migrations
bun run db:deploy

# 4. (Optional) Seed demo data
bun run db:seed

# 5. Start the stack
bun run dev

# 6. Verify:
#    - App:  http://localhost:3000
#    - API:  http://localhost:3001
#    - Agent: http://127.0.0.1:2000
```

If all three services start and the database shows "connected" in the API logs, the copy is functional.
---

## 12. Upload to GitHub Checklist

Before pushing to GitHub:

- [ ] `.env` is **not** committed (only `.env.example`)
- [ ] No `node_modules/` directory is present
- [ ] No `*.pem` files are present
- [ ] No Google OAuth client secrets or JSON credential files are present
- [ ] No `.eve/` or `.output/` build artifacts are committed
- [ ] `INSTALL.md` is present and accurate for the target audience
- [ ] `README.md` has been reviewed for accuracy
- [ ] `CHANGES.md` (this file) is included so reviewers understand what changed
- [ ] `LICENSE` is present (MIT)
- [ ] `SECURITY.md` is present and describes the security model
- [ ] The repo description accurately reflects that Docker is not required
---

*End of changes document.*

---

## 13. Telegram Channel Integration (2026-09-14)

### What changed

Added Telegram bot channel infrastructure to the research agent, enabling the agent to receive and respond to Telegram messages.

**New files:**
- `apps/agent/agent/channels/telegram.ts` — Telegram webhook channel using `defineChannel` with `POST /internal/telegram/webhook` route
- `apps/agent/agent/tools/send_telegram_message.ts` — Tool to send Telegram messages via the Bot API (`sendMessage` endpoint)
- `apps/agent/test/telegram-message.spec.ts` — Tests for `sendTelegramMessage` (3 tests: missing token, API error, success)

**Modified files:**
- `apps/agent/agent/tools/schedule_calls.ts` — Now imports `LEGAL_APPROACH_SCRIPT` and uses it as the default call reason instead of a hardcoded string
- `.env` — Added `TELEGRAM_BOT_TOKEN=""`
- `.env.example` — Added `TELEGRAM_BOT_TOKEN=""` documentation
- `.gitignore` — Added `.opencode/goals/` to ignore session state files

**Configuration:**
- Set `TELEGRAM_BOT_TOKEN`, `TELEGRAM_USERID`, `TELEGRAM_HOME_CHANNEL` in `.env` to enable Telegram notifications
- The channel receives webhooks at `POST /internal/telegram/webhook`
- Messages are dispatched through the existing `drainAll` pipeline

**Setting up the Telegram webhook:**

1. Get a bot token from [@BotFather](https://t.me/BotFather) and your numeric user ID from [@userinfobot](https://t.me/userinfobot)
2. Add `TELEGRAM_BOT_TOKEN`, `TELEGRAM_USERID`, `TELEGRAM_HOME_CHANNEL` to `.env`
3. Expose your API over HTTPS locally using `npx localtunnel --port 3001`
4. Register the webhook: `curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" -d "url=https://<LOCALTUNNEL_URL>/internal/telegram/webhook"`
5. Verify: `curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"`
6. To remove: `curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" -d "url="`

### Verification
- `bun test`: 204 pass, 9 fail (pre-existing database-related failures in `claimDue`, `dispatch lanes`, `retireExhausted`)
- `bun run build` (agent): ✓ Compiled successfully
- `bun run build` (app): ✓ Compiled successfully
- `bun run check-types`: ✓ Passes for agent, app, and api packages
- `bun test test/telegram-message.spec.ts`: 4/4 pass

---

## 14. Performance & Security Optimization (2026-09-16)

### What changed

Comprehensive optimization across 6 lanes per `IMPLEMENTATION_PLAN.md` (77 files, 634+/248-). Executed via parallel fixer agents under workflow manager.

**P1 — Security & Correctness:**

- `apps/agent/agent/lib/nonoh-sip.ts:13` — Removed hardcoded fallback `NO2026noh!` / `jarbcs` / `sip.nonoh.net`; password now throws `NONOH_PASSWORD is not set` if not configured; `isConfigured` checks env directly.
- `apps/api/src/config/env.validation.ts:102` — `AGENT_BRIDGE_SECRET` now `@MinLength(32)` with message `openssl rand -base64 32`; added `NONOH_SIP_SERVER`, `NONOH_USERNAME`, `NONOH_PASSWORD @MinLength(16)`, `NONOH_DISPLAY_NAME`.
- `apps/agent/agent/channels/voice.ts:88` — Replaced `=== Bearer` with `timingSafeEqual` constant-time; fail-closed when secret unset.
- `apps/api/src/google/sync.controller.ts:55` — Constant-time `timingSafeEqual` with dummy equal-length call on length mismatch (no length oracle).
- `apps/agent/agent/channels/crm.ts:17` + `eve.ts:44` — Unified fail-closed; `eve.ts` trims secret.
- `apps/app/app/api/[...path]/route.ts:14` + `apps/app/app/eve/v1/[...path]/route.ts` — Allow-list `/api/` / `/eve/` (404 otherwise), 1 MB `BODY_LIMIT` + `limitedBody` TransformStream streaming guard, `request.signal` passthrough, `console.warn` instead of `console.error`.
- `apps/api/src/create-app.ts:18` — `helmet({ contentSecurityPolicy: { defaultSrc ["'none'"], frameAncestors ["'none'"] }, crossOriginResourcePolicy: { policy: "cross-origin" }, hsts: production })`.
- `apps/api/src/app.module.ts:28` — `ThrottlerModule.forRoot([{ ttl: 60000, limit: 30 }])` + `APP_GUARD ThrottlerGuard` (global, `@SkipThrottle` for `/internal/sync/*`).
- `apps/app/next.config.ts:11` — `images.formats ["image/avif","image/webp"]`, `experimental.optimizePackageImports ["@carbon/icons-react","recharts"]`, `async headers()` CSP `default-src 'self'` + `frame-ancestors 'none'`, `X-Frame-Options DENY`, `X-Content-Type-Options nosniff`, etc.

**P1 — Dependencies (GHSA/Aikido):**

- Root + workspaces `better-auth 1.6.25 → 1.6.26` (Aikido enumeration), `@better-auth/sso` + `@better-auth/cli 1.4.22 → 1.6.26`, `next 16.2.12 → 16.3.5` (Snyk RCE/FileSystemCache + AVIF), `zod 4.4.3 → 4.5.0` (prototype pollution), `biome ^2.4.10 → ^2.5.6`; `overrides/resolutions { deepmerge-ts: ^8.0.1 }` for `GHSA-ggr8-5vv4-36mx`; kept `eve` in app (still used via `eve/react`) per grep.
- `package.json` + `apps/{app,api}/package.json` + `packages/auth/package.json` + `apps/agent/package.json` validated via `ConvertFrom-Json`; `bun.lock` not hand-edited (updates on next `bun install`).

**P2 — Performance:**

- `apps/api/src/google/gmail-sync.service.ts:237` — Chunked `Promise.all(5)` fetch + `Promise.allSettled(5)` store with warn logging (was sequential 120×7 DB ops).
- `apps/api/src/google/calendar-sync.service.ts:313/352` — Attendee upserts chunked 5-concurrency + `Promise.allSettled` `meetingSoon`.
- `apps/api/src/search/search.service.ts:18` — `length <3` guard + 10s `Map` TTL (200-entry LRU) cache.
- `apps/api/src/google/google-match.service.ts:44` — `TtlCache 300s` for `internalIdentity()`.
- `apps/api/src/trpc/list-input.ts:21` — `Math.min(page,100)` cap.
- `apps/api/src/companies/companies.service.ts:503` / `contacts:555` / `deals:358` — Facet cache key composite `JSON.stringify({q, filters})`.
- `apps/api/src/dashboard/dashboard.service.ts:39` + `ttl-cache.ts:20` — `delete(key)` + `invalidateSummary(scope)` per-user:scope.
- `packages/ui/src/lib/format.ts:7` — Module singletons `NF_USD`, `DF`, `NF_PCT` + `moneyCache` (was per-call `new Intl.*`).
- `apps/app/components/crm/record-sheet/record-sheet-host.tsx:14` — `setShown` moved to `useEffect`.
- `packages/ui/src/hooks/use-mobile.ts:1` — `useSyncExternalStore` (no hydration flicker).
- `packages/ui/src/components/chart.tsx:9` — `sanitizeColor` allow-list before `dangerouslySetInnerHTML`.

**P3 — Reliability:**

- `apps/api/src/deals/deals.service.ts:336` — `FACET_UNASSIGNED` now `ownerId=null` (was `in:[]` → 0 rows).
- `apps/api/src/workspace/workspace.service.ts:217,264` — `ORDER BY id FOR UPDATE` deterministic locking.
- `apps/agent/agent/lib/tasks.ts:62` — `jitteredBackoffMs` 0.85-1.15 + P2002 duplicate catch retry as update.
- `apps/agent/agent/lib/pool.ts:5` — `Map<string,A>` Set queue (all distinct `drainAll` args kept) + `console.warn` on catchUp.
- `apps/agent/agent/lib/dispatch.ts:25/88` + `portrait-sources.ts:125` — Structured `console.warn` (no silent swallow).
- `apps/agent/agent/channels/telegram.ts:156` — Fail-closed `return false` when `TELEGRAM_USERID` unset.
- `packages/db/src/favicon.ts:45` + `safe-fetch.ts:200` — `content-length` abort > `MAX_BYTES` (512KB / 3MB) before buffering; `safe-fetch` `MAX_BYTES=3MB`.
- `apps/app/proxy.ts:1` — 10s `gateCache Map` for `readOnboardingGate` (500-entry eviction).
- `apps/app/app/(app)/loading.tsx:1` — Streaming boundary skeleton.
- `packages/env/src/index.ts:32` — Warn on parse failures (not silent).

**Artifacts:**

- `IMPLEMENTATION_PLAN.md` — Full plan with lanes, evidence index (`gmail-sync:239`, `search:26`, etc.), verification gates.
- `apps/app/app/(app)/loading.tsx` — New.

### Why

Per code optimization scan `F:\crm` (~430 files): sequential Gmail N+1 (60s per tick), `contains insensitve` scans per keystroke, offset pagination, full user-table scan, per-call `Intl`, divergent dashboard cache, unbounded relations, hardcoded SIP secret, open proxy, missing rate-limit/CSP, weak bridge secret, timing oracle, CSS injection via `chart.tsx`, facet cache keyed only on `q`, `deals:336` `in:[]` bug.

### Verification

- `bun --filter=api check-types` / `--filter=agent` — pass (fix-1, fix-2, fix-6).
- `bun tsc --noEmit` `packages/ui`, `apps/app`, `packages/db`, `packages/env` — pass (fix-5, fix-6).
- `Select-String` no `NO2026|jarbcs|sip.nonoh` in `nonoh-sip.ts`; package.json `Select-String "2.4.10|16.2.12|1.6.25|1.4.22"` clean in owned scope.
- `git diff --stat` 77 files changed (see list above); `IMPLEMENTATION_PLAN.md` + `loading.tsx` untracked before commit.
